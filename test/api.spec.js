/* eslint-env mocha */

const chai = require('chai');

const { expect } = chai;

describe('API tests', () => {
  it('should run tests boilerplate', async () => {
    expect(true).to.be.eq(true);
  });
});
